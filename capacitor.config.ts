import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.hugh.deviceinspector',
  appName: 'Device Inspector',
  webDir: 'dist',
}

export default config
